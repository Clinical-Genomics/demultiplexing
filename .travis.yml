language: python
python:
    - "2.7"
    - "3.5"
services:
    - mysql

script:
    - py.test tests/
    - bash tests/test.bash

install:
    - pip install -r requirements.txt -e .

notification:
    - email: false
